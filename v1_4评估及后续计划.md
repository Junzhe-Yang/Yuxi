# 总体结论

我核对了压缩包中的最新代码、`2026-07-27-pea-rag-framework-systemic-analysis.md`，并逐步复盘了线上运行 JSON。当前失败的核心并非医学任务天然需要无穷无尽的特殊规则，也不主要来自向量检索质量。真正的问题在于：**系统把开放、重叠、条件化的临床语义过早编码成互斥、精确、顺序敏感的程序契约。**

本次运行中，系统成功抽取了3个处方要素，生成30个审查目标，完成8次向量检索和2次原文展开，保存29个唯一证据片段，Agent运行18步；检索证据已经覆盖特拉唑嗪、非那雄胺和氨氯地平的多个关键问题，但最终仍以`answer_generation_failed`结束。Agent决策、证据评估和答案生成合计消耗约43.4万tokens。

更具说明力的是，知识库已经找到了非常直接的患者特异证据，包括：

> 已有体位性低血压或血压过低的老年人应禁用α₁受体阻滞剂。

同一段还说明α₁受体阻滞剂与其他降压药联用会增强降压作用，并给出了减量、停药、换药和监测立卧位血压的处理原则。系统能够在对话中正确理解其临床意义，却因为该证据此前被评估为`conditional`、`applicability=uncertain`，或没有以精确的`element_id`绑定方式进入特定数组，连续五次拒绝Agent提交的判断。

因此，这次失败可以概括为：

```text
知识库找到了证据
→ Agent理解了证据
→ 证据评估器建立了某种绑定
→ 账本自动归类了一次
→ Agent又被要求手工重填证据角色
→ 程序按另一套精确规则再次校验
→ 提交失败
→ 完成门控把全部要素锁为证据不足
→ 最终模型重新生成出有临床内容的答案
→ 答案因改变锁定字段再次被整体拒绝
```

Yuxi原生ChatbotAgent的核心自主性来自模型决定检索什么、怎样改写查询、是否继续检索、是否打开原文和何时回答。新方法仍应保留这些行为，不需要退化为固定检索流程。

------

# 一、当前架构中的系统性问题

## 1. 同一临床语义存在五个“所有者”

当前系统中，以下组件都在部分地决定同一件事：

- 证据评估器决定相关性、适用性、极性和覆盖维度；
- 账本根据评估结果自动把Evidence ID加入不同数组；
- Agent再次决定判断和证据角色；
- 完成门控决定哪些项目能够进入答案；
- 答案生成器又重新组织判断、风险和建议；
- 答案验证器最后重新判断这些文本是否严格符合此前状态。

一旦多个组件的语义表示略有不同，就会出现程序性冲突。当前代码中，`merge_evidence_assessments`会保留同一证据的多个binding，而`_assessment_binding`校验时只返回遇到的第一个相同`element_id` binding。这意味着后续查询产生的更准确评估可能被旧binding覆盖，证据是否可用会受到合并顺序影响。

这种问题无法通过增加更多Prompt规则解决。每增加一个新角色、状态或校验条件，只会增加一次语义传递和新的不一致机会。

## 2. “一个chunk只有一种极性”的假设不成立

临床指南中的一段文字通常同时包含多个原子命题。上述α₁受体阻滞剂证据至少包含：

1. 药物类别会导致体位性低血压；
2. 老年人风险更高；
3. 已有体位性低血压时应避免使用；
4. 与其他降压药联用会增强降压作用；
5. 用药期间应监测立卧位血压；
6. 发生不良反应时应减量、停药或换药；
7. 某些高选择性药物对血压影响相对较小。

将整段证据压缩为单个：

```text
polarity = conditional
```

会丢失其中的禁忌、风险、监测和处理建议。随后再要求Agent把同一Evidence ID放进唯一正确的`support/challenge/condition/recommendation`数组，必然会产生冲突。

正确的表示单位应当是**证据中的原子claim**，一段证据可以形成多个claim，一个claim也可以同时承担风险说明和处理建议等作用。

## 3. 固定关系矩阵已实质变成固定工作流

当前代码为每种药物生成适应证、剂量、老年风险、监测和器官功能目标，并机械展开所有药物×疾病关系、所有药物对、重复用药、累积负担和处方遗漏。

本例仅有3种药物，却产生了30个审查目标；其中9个被标记为高优先级。高优先级目标若未检查，会阻止系统完成。其结果是：

```text
药物数量 × 疾病数量
+ 药物对组合
+ 每药固定维度
+ 病例级目标
```

成为近似强制的检索范围。

原本的“关系覆盖图”应当是帮助Agent理解病例的中间表示。当前实现把它变成了必须逐一关闭的状态空间。审计文档已经指出，完整任务要求覆盖治疗方案、正确与错误部分、调整建议和最终六段式答案；关系枚举只能作为其中一个辅助层。

## 4. 单一总体判断无法表达多方面合理性

多重用药审查中，一个药物常常同时具有正面和负面属性。例如本例中的特拉唑嗪可能出现：

- 用于BPH/LUTS的治疗方向成立；
- 当前2 mg给药可能位于常用剂量范围；
- 患者已有症状性体位性低血压和相关骨折史；
- 患者还在使用另一种降压药；
- 因患者特异风险，当前选择需要调整或避免。

如果每个要素只能保存一个：

```text
appropriate / inappropriate / needs_adjustment
```

前两个正确方面就容易丢失。反过来，如果要求“合理”必须只有support证据、“不合理”必须只有challenge证据，又会拒绝这种临床上常见的混合证据。

你现在的标准答案已经包括合理部分和不合理部分，因此运行时表示也应改成：

> 一个方案要素包含多个正面、负面和不确定finding，另有一个总体处置结论。

## 5. 完成性门控把“未完成调查”错误地转成了“没有证据”

预算耗尽后，当前`force_incomplete_state`会把未提交的方案要素统一改成`insufficient_evidence`，同时清空建议和监测字段。随后答案生成器被要求不得修改这些锁定状态。

线上轨迹中，最终模型实际上生成了三个药物的逐项评价、特拉唑嗪风险、非那雄胺适应证、氨氯地平剂量和联合低血压风险等内容。但验证器因为模型“新增了锁定状态中不存在的修正方案、监测要求和跨要素发现”而拒绝整份答案。最后网页只显示三个要素全部`insufficient_evidence`。

这使“证据不足”混合了至少三种完全不同的状态：

- 真实语料不足；
- Agent没有完成程序提交；
- 程序契约不接受已找到的证据。

在论文实验中，这三种情况必须分开。

## 6. 答案验证器正在执行不适合程序完成的语义任务

当前答案验证器要求：

- recommendation必须与账本字符串逐字相同；
- monitoring必须与账本字符串逐字相同；
- cross finding必须与此前提交对象一一对应且完全相等；
- 任何新增跨要素发现都视为非法；
- 对整段文本扫描具体数值。

其中数值正则还会把Evidence哈希中的`7b`、`718df`、`0fd`等识别成临床数值，从而产生“未由证据支持的具体数值”错误。线上JSON明确记录了这类误判。

程序适合校验：

- ID是否存在；
- source span是否真实；
- 是否缺少某个要素输出；
- 引用能否回溯；
- 结构化剂量值是否可在证据中找到。

程序不适合通过逐字相等判断两段医学建议是否语义一致。

## 7. 技术错误被混入临床推理预算

线上运行中有3次认证错误，它们被计为Agent第4、5、6步；最终答案修复又出现认证错误。技术故障由此消耗了18步逻辑预算，并最终影响临床输出。

应当分别维护：

```text
logical_agent_steps
technical_attempts
```

认证、超时和网关故障不能改变临床账本，也不能成为证据不足的理由。

------

# 二、方案应如何收敛

我建议保留PEA-RAG名称，将方法收敛为：

> **PEA-RAG v2：方案要素引导的动态证据探索与多方面综合**

其中：

- P：Plan-grounded，显式治疗方案要素；
- E：Evidence exploration，Agent自主证据探索；
- A：Aspect-level synthesis，方案要素的多方面综合。

新的方法流程为：

```text
病例及显式方案要素抽取
→ 动态审查议程
→ 自主检索Agent
→ 一次批量来源Claim抽取
→ 一次患者级多方面结构化综合
→ 局部验证和局部降级
→ 程序化六段式渲染
```

这个流程保留Agent自主决定查询、补检、打开原文和停止的能力，同时删除Agent作为“证据数据库录入员”的职责。

------

# 三、用少数通用约束替代不可数的特殊规则

“不列举特殊情况”不等于完全取消约束。应当把约束限制在跨疾病、跨药物都成立的结构层。

## 真正需要硬执行的约束

1. 每个显式方案要素必须有病例原文中的source span和稳定ID。
2. 每个显式方案要素最终必须恰好有一个`ElementReview`。
3. 引用的element、patient fact、evidence和claim ID必须真实存在。
4. 每条被引用claim的source span必须能够落回原始证据。
5. 明确的禁忌、停药、换药、具体剂量、阈值和疗程建议必须有对应来源；没有来源时删除具体细节或降级为一般性复核建议。
6. 技术错误与临床推理状态严格分离。

这些规则不依赖BPH、肺结核、糖尿病、肾病或某个具体药物。

## 只作为软判断或警告的内容

以下内容不再阻断最终答案：

- 某个动态审查问题没有调查；
- 某个非核心方面没有覆盖；
- 患者适用性仍存在不确定性；
- 正负弱证据发生冲突；
- 未取得某个正确项目的直接正向推荐；
- 检索达到预算；
- 推荐只能给出一般方向，无法给出具体替代药或剂量。

这里有一个重要方法学原则：

> **评价粒度不应直接变成控制粒度。**

金标准可以细分为适应证、剂量、疗程、定性问题、风险、建议等元事实，用于计算召回率和完整性。运行系统不需要把每一种元事实都变成硬编码的`required_dimension`。模型可以生成开放式finding，评价器再将finding与固定金标准事实匹配。

------

# 四、重新划分各模块的语义职责

## 1. 方案抽取器：只回答“原方案包含什么”

保留现有的：

- 病例事实抽取；
- 治疗方案要素；
- source grounding；
- 稳定ID；
- JSON Schema fallback；
- 一次修复。

删除或弱化：

- 针对所有时间表达的全局检查；
- 按方案类型强制列出不同必需维度；
- 试图在抽取阶段推断药物相互作用和临床合理性。

当前案例把“高血压10年”报告为未绑定的时间表达，就是全局时间规则缺少语境边界的例子。更合理的做法是只检查药品/医嘱区或已被抽取为方案要素的时间表达。

## 2. 检索Agent：只回答“接下来需要查什么”

Agent保留以下自主性：

- 选择优先调查的方案要素或跨要素问题；
- 生成临床命题式查询；
- 根据最新结果改写查询；
- 决定是否打开原文；
- 观察新增证据和重复率；
- 决定继续检索或结束。

工具压缩为：

```text
search_evidence
open_evidence_source
finish_retrieval
```

删除：

```text
submit_element_judgement
submit_cross_element_finding
request_finalize
```

`search_evidence`可以允许Agent一次提交1至3个自主生成的子查询。实验中按实际子查询数计算预算，而不是按工具回合数计算。这样既保留自主规划，也能避免一次查询、一次模型调用造成的步骤爆炸。

## 3. Claim抽取器：只回答“来源原文说了什么”

建议取消当前每轮执行的患者级`EvidenceAssessment`。检索过程中，Agent可以直接看到最新片段及简短上下文。Agent结束检索后，再对最终选定的10至15个唯一片段进行一次批量claim抽取。

推荐模型：

```python
class EvidenceClaim(BaseModel):
    claim_id: str
    evidence_id: str
    source_span: str

    linked_element_ids: list[str]
    linked_question_ids: list[str]

    statement: str

    role_hints: list[
        Literal[
            "supports",
            "raises_concern",
            "conditions",
            "recommends",
            "context",
        ]
    ]

    stated_conditions: list[str]
    stated_actions: list[str]
    numeric_facts: list[dict]
```

这里有三个关键变化：

- 一个Evidence可以产生多个claim；
- 一个claim可以有多个role hint；
- `role_hints`只帮助组织信息，不作为证据资格硬门控。

Claim抽取器只抽取来源中的条件，例如“已有体位性低血压”“与其他降压药联用”等，不提前决定这些条件是否满足当前患者。患者级适用性统一交给后续综合器，避免再次产生两个组件分别判断适用性的矛盾。

## 4. Review Synthesizer：唯一负责患者级判断

检索结束后执行一次结构化综合。输入为：

- 病例原文和患者事实；
- 全部显式方案要素；
- EvidenceClaim集合；
- Agent记录的未解决问题。

输出采用“多方面finding＋总体处置”：

```python
class ClinicalFinding(BaseModel):
    finding_id: str
    linked_element_ids: list[str]
    linked_patient_fact_ids: list[str]

    assessment: Literal[
        "appropriate",
        "concern",
        "uncertain",
    ]

    statement: str
    importance: Literal["major", "minor", "context"]

    evidence_ids: list[str]
    claim_ids: list[str]


class ElementReview(BaseModel):
    element_id: str
    findings: list[ClinicalFinding]

    overall_disposition: Literal[
        "continue",
        "continue_with_monitoring",
        "modify",
        "avoid",
        "uncertain",
    ]

    evidence_basis: Literal[
        "direct_support",
        "mixed_evidence",
        "no_material_conflict_found",
        "insufficient",
    ]

    recommendation: str | None
    recommendation_evidence_ids: list[str]

    monitoring: str | None
    monitoring_evidence_ids: list[str]

    missing_information: list[str]
```

药物—药物、药物—疾病、重复用药和处方遗漏不再需要单独提交对象。它们统一表示为`ClinicalFinding`：

- 关联一个要素时，是单要素finding；
- 关联两个或多个要素时，是跨要素finding；
- 关联疾病事实、但不关联现有药物时，可以表示处方遗漏。

这会显著减少Schema和工具数量。

------

# 五、如何评价“正确部分”

此前要求每个合理项目必须取得正向证据，在受控指南库中有一定合理性，但对每个常规处方细节都强制检索正向推荐，会重新引发查询爆炸。

建议区分四种证据状态：

| 判断基础                     | 含义                                               |
| ---------------------------- | -------------------------------------------------- |
| `direct_support`             | 有直接指南证据支持该方案或细节                     |
| `mixed_evidence`             | 方案存在合理方面，也存在患者特异风险或限制         |
| `no_material_conflict_found` | 已开展相关检索，未发现实质冲突，但缺少直接正向推荐 |
| `insufficient`               | 语料或患者信息不足，无法形成可靠判断               |

因此，最终答案可以说：

- “该剂量与检索到的推荐范围一致”；
- “当前检索未发现该项本身存在明确不合理，但缺少患者特异正向依据”；
- “适应证成立仍依赖尚未提供的前列腺体积或PSA信息”。

这比强行把所有内容二分为“证据证实合理”和“不合理”更符合临床证据边界。

论文评价时，可以将`direct_support`和正确的`no_material_conflict_found`都映射为金标准中的“正确项”，同时单独报告**直接支持覆盖率**。这样既能评价完整答案，也不会夸大系统的证据强度。

------

# 六、局部验证与局部降级

新验证器应只做局部处理。

### 引用错误

若某finding引用了不存在的Evidence ID：

- 删除无效引用；
- 如果仍有其它合法证据，保留finding；
- 如果负向结论完全失去证据，将该finding降为`uncertain`。

### 具体建议无来源

若模型提出具体替代药、剂量、频率或疗程，但引用claim不支持：

- 删除具体药名或数值；
- 改为“建议由相关临床医师或药师评估停用、替换或调整方案”；
- 保留已被证据支持的风险判断。

### source span不匹配

删除该claim，不影响同一Evidence中的其它合法claim，也不影响其它方案要素。

### 证据冲突

保留冲突，输出`uncertain`或`mixed_evidence`，不要求程序选择唯一极性。

### 某个要素输出缺失

程序补充：

```text
overall_disposition = uncertain
evidence_basis = insufficient
```

同时记录错误，不拒绝其它要素。

### 数值校验

不要再对包含Evidence哈希的完整自由文本运行正则。只检查结构化字段中的：

- 剂量；
  -单位；
  -疗程；
  -频次；
  -阈值；
  -时间点。

Evidence ID应在校验前完全剔除。

------

# 七、建议的代码改造

| 当前模块                       | 建议处理                                                     |
| ------------------------------ | ------------------------------------------------------------ |
| `review_targets.py`            | 移除运行时固定矩阵和`ELEMENT_DIMENSIONS`；替换为`review_agenda.py`动态问题 |
| `evidence_assessment.py`       | 改为检索结束后的批量`claim_extraction.py`                    |
| `ledger.py`                    | 删除四类Evidence ID角色数组和判断提交校验；替换为简单`evidence_board.py` |
| `submit_element_judgement`     | 删除                                                         |
| `submit_cross_element_finding` | 删除                                                         |
| `completion_guard.py`          | 替换为轻量`pre_synthesis_check.py`                           |
| `force_incomplete_state`       | 删除                                                         |
| `answer_generation.py`         | 删除锁定骨架、逐字相等和全局数值正则；替换为`review_synthesis.py` |
| 自由文本第二次润色             | 删除；结构化结果直接程序化渲染                               |
| 技术重试                       | 与logical agent step分开计数                                 |

保留的部分包括：

-病例与方案要素抽取；
-source grounding和稳定ID；
-Milvus直接Retriever；
-结构化错误状态；
-evidence ID和来源；
-trace与批处理；
-每题独立thread。

------

# 八、最可行的实施顺序

## P0：先用现有JSON验证后半段

这是当前最重要的一步，无需重新运行任何检索。

直接从线上JSON读取：

-患者事实；
-3个方案要素；
-29个唯一Evidence；
-相关原文。

随后绕过：

```text
submit_element_judgement
ledger judgement
completion guard
locked answer skeleton
```

直接运行：

```text
批量Claim抽取
→ 一次结构化Review综合
→ 局部验证
→ 六段式渲染
```

P0的验收标准：

-三个方案要素均产生结果；
-特拉唑嗪同时保留适应证/剂量方面和患者特异风险方面；
-非那雄胺剂量依据与适应证条件分别表达；
-氨氯地平剂量和总体降压负担分别表达；
-缺少来源的具体替代剂量被局部降级；
-任何局部失败不再导致整例失败。

P0通过后，才能继续调整Agent检索。

## P1：删除手工判断提交

将Agent工具收缩为`search/open/finish`，保留当前向量检索和8次子查询预算。

## P2：用动态议程替换30个固定目标

由一次病例级规划生成3至8个自由文本`ReviewQuestion`。程序只检查引用ID，不检查临床问题类型。Agent可以新增、合并或推迟问题。

未调查问题只进入warning，不能阻止最终综合。

## P3：批量Claim抽取和状态压缩

Agent每轮只看到：

-紧凑患者摘要；
-方案要素；
-动态问题；
-已有Evidence索引；
-最新片段；
-剩余预算。

相同Evidence在同一病例中只分析一次。

## P4：完成局部降级与技术错误隔离

-认证和超时不计逻辑步骤；
-删除Evidence哈希数值误判；
-最终修复调用失败时保留第一版结构化草稿；
-局部错误只降级局部输出。

------

# 九、建议的增量实验

所有实验组都必须生成完整六段式答案，避免再次产生只有检索、没有回答的半成品。

| 组别                                     | 方案要素 | Agent自主检索 | Claim卡片 | 最终结构化综合 |
| ---------------------------------------- | -------- | ------------- | --------- | -------------- |
| B0：Yuxi ChatbotAgent                    | 否       | 是            | 否        | 原生回答       |
| B1：ChatbotAgent＋方案要素简表           | 是       | 是            | 否        | 原生回答       |
| M1：方案要素＋自主检索＋直接从chunks综合 | 是       | 是            | 否        | 是             |
| M2：PEA-RAG v2完整方法                   | 是       | 是            | 是        | 是             |

统一条件：

-同一Agent模型；
-同一向量知识库；
-vector-only；
-top-5；
-最多8个实际子查询；
-最多2次open；
-相同最终证据token预算；
-同一六段式输出要求。

当前v1.4可以在小型开发集中保留为架构失败对照，但不适合作为全数据集主方法组，其成本和失败率过高。

主要指标建议包括：

1. 方案要素Precision、Recall；
2. 金标准finding级Micro/Macro Precision、Recall、F1；
3. 正确项与不合理项分层F1；
4. 属性完整率；
5. 完整finding率；
6. 引用蕴含率；
7. 患者适用性正确率；
8. 无来源具体建议率；
9. 最终答案生成成功率；
10. 整例失败率；
11. 子查询数、open次数、唯一证据增益；
12. tokens、延迟和成本。

当前单病例总消耗约43.4万tokens，第一阶段工程目标应设为至少下降60%，并消除因`EvidenceContractError`导致的整例失败。

------

# 十、最终应避免的方向

后续不应继续增加：

-新的`ReviewTargetType`；
-新的`required_dimensions`；
-BPH、肺结核、糖尿病、肾病等病例专用分支；
-针对每个药物类别的硬编码证据角色；
-更多`support/challenge/condition/recommendation`提交数组；
-更多完成门控条件；
-更多自由文本逐字相等校验；
-更多用于修复前述约束的Prompt规则。

这些改动只能让系统在少数开发病例上暂时通过，并会在新的疾病、方案类型或指南表达下继续失败。

应当收敛到一个更清楚的职责划分：

> 方案要素决定必须评价哪些对象；
> Agent决定检索哪些证据；
> Claim抽取器忠实表示来源说了什么；
> 单一综合器决定这些证据对患者意味着什么；
> 程序只保证结构、引用和局部安全降级。

这套设计保留了Agentic RAG的实质自主性，也避免把临床医学的开放世界问题转化为不可维护的规则枚举。它还更贴合你已经完善的完整金标准：正面和负面信息都表示为finding，最终整体判断由多个finding综合得到，而不会因为一个证据角色或字段绑定错误而丢失整份答案。

相关文件：

[压缩包内的PEA-RAG系统性分析](sandbox:/mnt/data/Yuxi_extracted/Yuxi/docs/vibe/2026-07-27-pea-rag-framework-systemic-analysis.md)

[PEA-RAG v2收敛设计与实施报告](sandbox:/mnt/data/2026-07-27-pea-rag-v2-convergence-design.md)

[本次线上运行完整JSON](sandbox:/mnt/data/e3f7d41c-a610-4037-af7f-e778a83171e2.json)