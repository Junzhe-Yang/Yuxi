你的质疑是正确的。**上一版“薄Harness＋方案锚点＋可读Evidence Card”如果直接作为论文主方法，确实过度纠偏，场景针对性和方法贡献都不够。** Evidence Card解决的是工具结果不可见、固定前缀截断等工程缺陷；它应当成为所有实验组共享的公平运行条件，不能单独被包装成老年多重用药方法创新。

原始Yuxi已经具备模型自主决定检索、查询改写、多轮补检、打开原文和停止回答的能力。仅在前面加一份方案清单、后面查一下有没有漏写，和baseline的实质差异有限。

同时，前一版复杂PEA-RAG已经证明，临床相关性、证据极性、患者适用性、判断资格和建议资格一旦由多个模型与程序规则反复裁决，就可能把已经召回且被Agent正确理解的证据否决。本次特拉唑嗪案例中，知识库明确召回“已有体位性低血压的老年人应禁用α₁受体阻滞剂”，Agent也形成了正确的患者特异判断，最终却多次遭遇`EvidenceContractError`。

因此，合理的中间路线应当遵循一个明确原则：

> **硬编码任务拓扑，保留临床语义自主性。**

所谓任务拓扑，是老年多重用药审查始终具有的结构：患者有若干治疗方案要素，也有若干可能改变用药合理性的患者特征；系统需要在两者之间寻找重要联系，并完整评价原方案。所谓临床语义，是究竟哪种药物与哪个疾病或风险有关、证据是否适用、结论应如何形成，这部分应继续交给Agent，而不能由程序预设。

---

# 一、重新确定研究问题

根据目前的实验，真正有证据支持的问题发现包括三个层面。

第一，向量检索、向量＋BM25和通用图谱检索之间的差异并非当前主要性能瓶颈。高质量向量检索已经能找到不少关键证据。

第二，自由ReAct Agent的主要缺陷是**缺少持久化的病例级审查状态**。它可以动态检索，却不能稳定记住：

* 哪些原始治疗方案要素尚未评价；
* 哪些患者特异风险已经与哪些药物发生联系；
* 哪些检索只是重复搜索；
* 哪些证据已经足以支持后续回答；
* 当前答案是否遗漏了某一治疗方案部分。

Yuxi现有源码分析也明确指出，baseline具有动态查询和多轮检索能力，但没有关系覆盖保证，停止点容易受模型影响，查询与结果难以按治疗方案要素对齐。

第三，PEA-RAG V2的问题是**检索后信息损耗**。Evidence已经找到，后续的绑定、角色分类、Claim转写、适用性校验和答案契约却将其删除或降级。

由此可以将论文研究问题正式收束为：

> 在不限制Agent自主检索和临床判断的前提下，患者—治疗方案的持久化覆盖记忆能否减少老年多重用药审查中的漏项、注意力漂移、重复检索和过早停止？

这个问题明确、可证伪，也与通用Yuxi Agent有实质区别。

---

# 二、建议的新方法：患者—方案交互记忆引导的Agentic RAG

建议命名为：

> **Patient–Regimen Interaction Memory-guided Agentic RAG，PRIM-RAG**
> **患者—方案交互记忆引导的Agentic RAG**

方法不再构建固定的药物×疾病笛卡尔积，也不再为每个对象写死适应证、剂量、肾功能、监测、相互作用等必查字段。

它只维护两个病例特异的对象集合。

## 1. 治疗方案锚点

治疗方案锚点表示病例明确提出、最终必须评价的内容：

```python
class PlanAnchor(BaseModel):
    plan_id: str
    source_span: str
    normalized_text: str
    kind: Literal[
        "medication_order",
        "regimen_or_combination",
        "explicit_timing_or_duration",
        "explicit_monitoring_or_followup",
        "other_explicit_plan",
    ]
```

至少包括：

* 每一条药物医嘱；
* 明确提出的联合治疗方案；
* 明确的方案级疗程或复评时点；
* 明确的监测、随访、停药或换药安排。

这部分是硬覆盖对象，因为你们的金标准本身要求每个明确治疗方案要素恰好评价一次。审计文件也明确表明，审查对象应覆盖药物、联合方案、治疗方向、疗程、评估和监测，而不能缩减为药物—疾病关系集合。

治疗方案锚点只用于防漏，不限定Agent还能发现什么。Agent仍可以主动发现：

* 药物—药物相互作用；
* 药物—疾病冲突；
* 累积药理负担；
* 处方遗漏；
* 原解析器未识别的方案级问题。

## 2. 患者修饰因素锚点

这是老年多重用药场景的核心针对性所在。

```python
class PatientModifier(BaseModel):
    modifier_id: str
    source_span: str
    normalized_text: str
```

抽取Prompt只要求：

> 从病例原文中提取可能改变药物适应证、选择、剂量、疗程、相互作用风险或监测要求的患者事实。

不预先列出具体疾病和药品规则，也不要求固定分类。对当前BPH病例，可以自然形成：

```text
PM001：卧位150/90 mmHg，立位110/70 mmHg并伴头晕
PM002：既往起床晕倒并发生股骨颈骨折
PM003：高血压病史
PM004：IPSS 18分
PM005：残余尿量35 ml
PM006：肝肾功能缺失
```

这些对象均来自病例原文，不包含外部医学判断。

## 3. 动态患者—方案交互记忆

系统不预先枚举全部：

[
P\times M
]

组合。Agent在检索过程中自行选择值得调查的稀疏联系。

例如：

```text
PE001 特拉唑嗪
  ↔ PM001 体位性低血压
  ↔ PM002 晕厥骨折史
  ↔ PE003 氨氯地平
```

Agent调用检索工具时，可以附带其当前关注对象：

```python
search_review_kb(
    query_text: str,
    focus_plan_ids: list[str] = [],
    focus_modifier_ids: list[str] = [],
    reason: str = "",
)
```

这些ID只承担三项作用：

* 建立trace；
* 更新持久化覆盖记忆；
* 在后续轮次提醒Agent已经查过什么。

它们不参与Evidence资格校验。即使Agent错误地漏填ID，检索仍正常执行，程序也不会拒绝结果。

状态可以极简地表示为：

```python
class CoverageEntry(BaseModel):
    plan_id: str
    linked_modifier_ids: list[str] = []
    query_ids: list[str] = []
    evidence_ids: list[str] = []
    mentioned_in_draft: bool = False
```

于是每轮Agent都能看到：

```text
PE001 特拉唑嗪
- 已调查：PM001体位性低血压、PM002既往晕厥骨折
- 已检索：Q001、Q003
- 已获得：EV007、EV011
- 最终回答：尚未覆盖

PE002 非那雄胺
- 已调查：适应证和当前方案条件
- 已获得：EV015
- 最终回答：尚未覆盖

PE003 氨氯地平
- 尚未形成患者特异调查
```

这不是临床规则，也不判断哪个Evidence是支持、反对或条件性证据。它只是Agent的外部工作记忆。

形式化地，可以写为：

[
\mathcal{M}_t=
\left(
P,,
C,,
L_t,,
E_t,,
A_t
\right)
]

其中：

* (P)为治疗方案锚点；
* (C)为患者修饰因素；
* (L_t\subseteq P\times C)为截至第(t)轮由Agent主动调查的稀疏联系；
* (E_t)为已获得的证据；
* (A_t)为方案要素是否进入当前回答的结构状态。

Agent策略变为：

[
a_t\sim
\pi_\theta
\left(
a\mid x,\mathcal{M}*t,o*{1:t}
\right)
]

临床查询、证据判断和最终结论仍由同一个Agent完成。

---

# 三、方法中应保留和删除的约束

科学上最重要的边界如下。

| 适合硬约束的内容             | 应交给Agent判断的内容        |
| -------------------- | -------------------- |
| 病例事实必须有原文source span | 哪个患者事实会影响哪个药物        |
| 明确方案要素具有稳定ID         | 是否存在药物—疾病冲突          |
| 每个明确方案要素最终至少评价一次     | Evidence是否支持、反对或附带条件 |
| Evidence ID必须真实存在    | Evidence是否适用于当前患者    |
| 检索与open预算            | 临床严重程度               |
| 技术错误与逻辑步骤分开记录        | 是否需要换药、减量或监测         |
| 最终依据清单可回溯            | 如何综合正面和负面证据          |
| 最多一次覆盖补救             | 是否还需要进一步检索           |

需要明确删除：

* 固定药物×疾病全组合；
* 固定`ReviewTargetType`矩阵；
* `support/challenge/condition/recommendation`证据数组；
* 外部Evidence极性裁判；
* Evidence适用性程序门槛；
* `submit_element_judgement`证据合同；
* Claim二次抽取；
* Source span由LLM重复转写；
* 程序化医学结论降级；
* 对自由文本进行临床数值正则否决。

这能避免再次出现“Agent读懂了证据，程序却不允许它使用”的情况。当前trace中，直接禁忌证已经被召回，Agent也反复形成患者特异性判断，但证据合同仍将其判为“不相关或不适用”。 

---

# 四、仍然保留的场景针对性在哪里

PRIM-RAG与普通Yuxi Agent的区别，不依赖新增大量临床规则，而体现在三个老年多重用药特异机制。

## 1. 双轴病例表示

通用问答Agent主要围绕一个自然语言问题检索。

PRIM-RAG围绕两个集合工作：

[
\text{治疗方案要素}
\times
\text{患者修饰因素}
]

多重用药合理性本身就是判断这些对象之间是否存在支持、限制、冲突或条件关系。方法不枚举所有边，但显式保留两端节点及Agent调查过的边。

## 2. 持久化覆盖状态

自由Agent的推理计划只存在于消息历史和隐式注意力中。病例复杂、检索轮次增加后，它可能反复调查最显眼的高风险药物，漏掉其它正确或条件性合理项目。

PRIM-RAG把“已经调查哪些方案要素、关联过哪些患者事实、获得哪些Evidence”保存在状态中，并在每轮模型调用前显示。

这是运行时机制，不只是系统Prompt。

## 3. 一次有界的覆盖反思

当Agent首次产生最终答案时，程序只检查：

* 所有明确`PE`是否出现；
* 引用的Evidence ID是否存在。

如果存在遗漏，程序向同一个Agent返回：

```text
当前草稿未评价PE002和PE005。
请根据已有证据判断是否可以直接补充；
证据不足时可继续检索；
不得删除或重写已经完成的其它部分。
```

Agent获得一次额外循环机会，可以自行：

* 使用已有Evidence补写；
* 再检索；
* 明确资料不足。

程序不检查判断是否正确，也不要求固定证据角色。

因此，这个模块针对的是论文中已经观察到的“5个要点只回答4个”的完整性问题。

---

# 五、Evidence Card应成为共同基础设施，而非创新点

最新失败案例说明，Agent如果只能看到Evidence ID或固定前260字符，就无法构成真正的观察—行动循环。这个问题必须修复，但应当对baseline和新方法同时修复。

所有实验组统一使用：

```json
{
  "evidence_id": "EV007",
  "source": "老年BPH/LUTS药物治疗共识（2015）",
  "chunk_index": 12,
  "excerpt": "已有体位性低血压或血压过低的老年人应禁用α1受体阻滞剂……与其他降压药合用，降压作用增强……用药期间建议监测立卧位血压。"
}
```

Excerpt通过查询中心窗口生成：

1. 清理HTML和LaTeX；
2. 按句子、段落和表格行切分；
3. 根据当前查询词定位最高匹配窗口；
4. 返回窗口及相邻内容；
5. 完整原文保存在Evidence Store中；
6. Agent可按Evidence ID调用open。

这样可以保证主实验中的差异来自PRIM覆盖记忆，而非某个组看到了更好的工具结果。

---

# 六、可检验的科学假设

建议将论文的假设限定为三条。

### H1：治疗方案覆盖假设

> 在相同模型、检索后端和查询预算下，治疗方案覆盖记忆能够提高显式方案要素召回率、完整Finding率和完整病例正确率。

预期主要改善：

* 漏掉某个药物；
* 漏掉联合方案；
* 漏掉疗程或评价时点；
* 只报告不合理部分，遗漏合理部分。

### H2：患者特异联系假设

> 在治疗方案覆盖记忆基础上，加入患者修饰因素及动态交互记忆，能够提高依赖患者条件的用药问题召回率和证据患者适用性。

预期主要改善：

* 药物与既往跌倒、谵妄、低血压等风险的联系；
* 药物与肾肝功能、实验室指标的联系；
* 多种药物对同一不良结局的累积影响；
* 规则存在但患者条件不满足的误判。

### H3：有界覆盖反思假设

> 在首次回答后提供一次未覆盖方案要素反馈，能够提高属性完整率和完整Finding率，且不会显著增加无依据临床论断。

这三条假设均可被实验否定。

如果M1没有提高方案覆盖，说明方案记忆没有价值；如果M2没有提高患者条件相关Finding，患者修饰因素模块应删除；如果一次覆盖反思只增加token而没有提高完整性，也不应保留。

---

# 七、实验设计

所有组均生成完整最终答案，避免再构造无法评价的半成品。

| 组别              | Evidence Card | 方案覆盖记忆 | 患者修饰因素 | 一次覆盖反思 |
| --------------- | ------------: | -----: | -----: | -----: |
| B0：原始Yuxi       |        原始工具形式 |      否 |      否 |      否 |
| B1：公平运行baseline |             是 |      否 |      否 |      否 |
| M1：Plan-Memory  |             是 |      是 |      否 |      否 |
| M2：PRIM         |             是 |      是 |      是 |      否 |
| Full：PRIM-RAG   |             是 |      是 |      是 |      是 |

论文中最主要的受控比较应为：

[
B1 \rightarrow M1 \rightarrow M2 \rightarrow Full
]

B0用于说明原始框架实际表现；B1排除Evidence展示缺陷；M1检验方案覆盖；M2检验患者特异性；Full检验覆盖补救。

统一：

* 同一Agent模型；
* 同一向量知识库；
* 同一top-(k)；
* 同一最大实际子查询数；
* 同一open预算；
* 同一系统输出要求；
* 同一最终上下文上限；
* 每题独立thread。

## 一个很有价值的低成本阴性对照

在部分开发病例上增加：

> **Token-matched generic checklist**

向Agent提供长度相近的通用用药检查提示，但不给患者特异的Plan和Modifier记忆。

若PRIM-RAG优于等长通用Checklist，能够说明增益来自病例特异结构，而非单纯增加Prompt长度。

还可以在少量病例上使用“打乱患者修饰因素”的阴性对照。若把另一病例的风险因素放入当前病例后性能下降，能够进一步支持患者特异记忆机制，而非任意额外上下文带来的效果。

---

# 八、评价指标

主要结果继续使用你们目前完整的金标准：

* Finding级Micro-F1和Macro-F1；
* 合理项F1；
* 不合理项F1；
* 属性完整率；
* 完整Finding率；
* 完整病例正确率。

机制指标建议增加四项。

### 1. 方案覆盖率

[
\text{Plan Coverage}
====================

\frac{
\text{被最终答案评价的金标准方案要素数}
}{
\text{全部金标准方案要素数}
}
]

### 2. 患者修饰因素相关Finding召回率

[
\text{Modifier-linked Recall}
=============================

\frac{
\text{正确识别的患者条件依赖Finding数}
}{
\text{全部患者条件依赖金标准Finding数}
}
]

该指标可以只在40个复杂病例上标注，不必重做全部数据集。

### 3. 检索后事实存活率

[
\text{Post-retrieval Survival}
==============================

\frac{
\text{最终回答正确保留的已召回金标准事实数}
}{
\text{检索结果中已存在的金标准事实数}
}
]

它能够直接衡量PEA-RAG V2的核心失败：证据已经召回，却在后处理阶段消失。

### 4. 查询重复率

[
\text{Query Redundancy}
=======================

\frac{
\text{与既有查询高度重复且无新增金标准证据的查询数}
}{
\text{全部查询数}
}
]

另外报告：

* 首次回答遗漏方案要素的病例比例；
* 一次覆盖反思的缺口关闭率；
* Citation蕴含率；
* Citation患者适用率；
* 无来源具体建议率；
* 检索调用数；
* tokens、延迟和费用；
* 整例失败率。

---

# 九、在Yuxi中的最小实现

不需要继续维护大型自定义状态图。建议在`create_agent`框架上增加三个中间件。

## 1. `CaseMemoryMiddleware`

首次运行时抽取：

```python
plan_anchors
patient_modifiers
```

后续每轮把紧凑记忆注入模型：

```text
明确方案要素：
[PE001] 特拉唑嗪2 mg qn
[PE002] 非那雄胺5 mg qd
[PE003] 氨氯地平5 mg qd

患者修饰因素：
[PM001] 明确体位性低血压并伴头晕
[PM002] 既往起床晕倒并发生骨折
[PM003] 肝肾功能未提供

调查状态：
PE001：已关联PM001/PM002；已获得EV007
PE002：已获得EV015
PE003：尚未形成患者特异调查
```

## 2. `ReviewEvidenceTool`

包装现有Milvus Retriever：

```python
search_review_kb(
    query_text,
    focus_plan_ids=[],
    focus_modifier_ids=[],
    reason="",
)
```

返回Evidence Card，同时更新Coverage Memory。

不调用外部Evidence Judge。

## 3. `CoverageFinalizerMiddleware`

当模型第一次产生无工具调用的最终回答：

```python
missing = plan_ids - plan_ids_mentioned_in_answer
```

若`missing`非空且尚未反思：

* 追加一次覆盖缺口消息；
* 返回同一个Agent循环；
* Agent可自行检索或补写。

第二次回答直接结束。

整个系统只有一个临床语义主体：Agent模型。

---

# 十、最终定位

上一版V3“薄Agent”只解决了工程性左右脑分裂，不能独立承担论文方法贡献。它应降级为公平运行基础设施。

真正适合当前论文的方法应当是：

> **以完整治疗方案要素为硬覆盖对象，以患者修饰因素为软临床线索，以Agent自主选择的稀疏患者—方案联系为检索路径，并通过持久化覆盖记忆和一次有界反思减少漏项与过早停止。**

这套方法既没有退回原始baseline，也没有恢复数十个硬编码临床模块。它编码的是老年多重用药任务的结构，而非枚举具体医学答案。

论文的核心论述可以形成一条连贯链条：

1. 通用图谱和BM25没有显著改善最终性能；
2. 自由Agent具备动态检索能力，但缺少病例级覆盖状态；
3. 复杂规则流水线会造成检索后信息损失；
4. 因此提出患者—方案交互记忆；
5. 该记忆保持临床语义自主，同时显式管理方案覆盖和患者特异联系；
6. 通过逐组件消融验证其是否真正改善查全、查准、完整性和效率。

这才是目前最符合已有实验事实、具有明确科学假设、同时能够落地实现的研究方向。
