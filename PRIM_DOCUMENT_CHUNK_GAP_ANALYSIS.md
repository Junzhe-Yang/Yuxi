# PRIM-RAG 文档召回与块召回差异分析

## 先澄清比较口径

主分析严格使用 PRIM-RAG 与 baseline 共有的 175 题、Agent 自主停止前的全部
成功搜索调用，并且每次只看原始 Top-5。金标准块仍只来自答案末尾“依据清单”，
通过 `medium/high` 自动映射进行判断。

按这套一致口径重新从块所属文档计算：

| 指标 | PRIM-RAG | baseline | PRIM - baseline |
|---|---:|---:|---:|
| Macro 文档召回 | 93.67% | 93.45% | +0.22 个百分点 |
| Micro 文档召回 | 89.88%（293/326） | 88.34%（288/326） | +1.53 个百分点 |
| 全部目标文档覆盖率 | 85.14% | 81.71% | +3.43 个百分点 |
| Macro 块召回 | 67.67% | 69.02% | -1.35 个百分点 |
| Micro 块召回 | 61.80%（550/890） | 63.71%（567/890） | -1.91 个百分点 |

PRIM 文档召回的宏平均优势只有 0.22 个百分点，配对 bootstrap 95% CI 为
`[-2.27, 2.60]`，不能认为 PRIM 已稳定优于 baseline。175 题中 PRIM 文档召回
更高 15 题、相同 150 题、更低 10 题。此前若看到明显更高的 PRIM 文档召回，
需要检查是否混用了 PRIM 的 425 条全量结果、不同金标层级或没有统一 Top-5。

不过，“PRIM 文档层面略好、块层面略差”这个方向在当前结果中确实存在，以下
分析解释它为何可能发生。

## 根本原因：两个指标的任务难度完全不同

175 题共有 326 个唯一目标文档，却有 890 个手工目标块，平均每个目标文档承担
2.73 个证据目标。90 题只有一个目标文档，其中 84 题仍需要命中多个不同块。

因此：

- 文档召回只要求同一文档的任意一个 Top-5 块出现一次；
- 块召回要求在同一文档内部命中多个具体证据位置；
- 多搜索几次很容易使文档变成“已命中”，却不一定增加新的目标块。

PRIM 的目标级转化漏斗进一步证明了这一点：

| 转化阶段 | PRIM-RAG | baseline |
|---|---:|---:|
| 目标块所属文档曾出现 | 845/890（94.94%） | 835/890（93.82%） |
| 文档已出现后，目标块也命中 | 550/845（65.09%） | 567/835（67.90%） |
| 块漏召中发生在已命中文档内部 | 295/340（86.76%） | 268/323（82.97%） |

PRIM 的 340 个块漏召中，只有 45 个是文档本身没找到；295 个已经进入正确文档，
但拿错了段落。问题主要是文档内证据定位，而不是文档选择。

## 调用记录显示的四类问题

### 1. 查询过度围绕单一风险关系，不按未覆盖证据目标推进

PRIM 会先构造关系问题并反复改写。例如 gid 48 的五次查询始终集中在
“瑞舒伐他汀/高强度他汀导致新发糖尿病风险”。它反复命中同一个手工目标
`血脂共识#15`，也命中 `#11`，但从未主动覆盖依据清单中的 `#8/#9/#10`。
baseline 使用更宽的“他汀、血脂、血糖偏高、用药选择”查询，第三次调用一次
覆盖了 `#8/#10/#11`。

这说明 PRIM 当前的关系分解提高了局部论点的深挖，却没有维护“哪些 evidence
obligation 仍未覆盖”的显式状态。查询多样性表现为措辞变化，不一定是证据目标
多样性。

### 2. 进了正确文档后仍缺少文档内定位/邻域展开

对 PRIM 的 295 个“文档命中、目标块未命中”目标，平均已经检索了同文档的
6.54 个不同自动块、跨 3.95 次调用：

- 155 个目标已经看过同文档 6 个以上的不同块；
- 58 个看过 4–5 块；
- 56 个看过 2–3 块；
- 只有 26 个仅看过 1 块。

这不是简单的“文档没进 Top-5”。系统已经知道正确文档，却继续依赖全库语义
检索，没有切换成“限定文档 + 章节/关键词检索”或从高分块向相邻块展开。

gid 141 是典型例子：PRIM 多次进入《老年心房颤动共识》，但返回的五个不同块
没有命中金标 `#18/#19/#20`；baseline 用更短、更直接的
“多潘立酮 QTc 延长…”和“胺碘酮 QTc…”查询，使这些块进入 Top-5。

### 3. “新 evidence_id”被当作进展，但多数没有新增目标覆盖

175 条 PRIM 记录中：

- 175/175 的 coverage section 解析均退化；
- 174/175 在 `coverage_report.missing_after_reflection` 中仍有 PE 项；
- 这 174 条的 `reflection_report.triggered` 全部为 `false`；
- 1,239 次调用中，821 次（66.26%）虽然产生了新的 evidence ID，却没有新增
  任何金标目标块。

晚期调用尤其明显：

| 调用阶段 | 新增目标的调用比例 | 每调用新增目标 | 金标块位置占比 | 非目标文档位置占比 |
|---|---:|---:|---:|---:|
| 第 1–3 次 | 46.56% | 0.742 | 31.79% | 44.47% |
| 第 4–5 次 | 22.43% | 0.283 | 29.22% | 46.17% |
| 第 6 次以后 | 13.45% | 0.178 | 20.96% | 53.91% |

也就是说，内部去重只证明“拿到了此前没见过的块”，没有证明“填补了此前没
覆盖的论点”。这会让自主 Agent 继续扩张证据池，却不收敛到缺失证据。

### 4. 更多调用扩大了文档覆盖，也扩大了无关结果

175 题上 PRIM 检查了 6,195 个 Top-5 位置，baseline 检查 4,360 个。按位置分类：

| Top-5 位置类型 | PRIM-RAG | baseline |
|---|---:|---:|
| 金标自动块别名 | 27.68% | 40.28% |
| 正确文档内的非目标块 | 24.41% | 23.60% |
| 其他文档块 | 47.91% | 36.12% |

PRIM 的额外调用确实提高了“至少见过目标文档”的概率，但新增结果有更大比例落在
其他文档。这正好能抬升低分辨率的文档 union recall，却不能抬升块召回。

需要注意：PRIM 的底层接口每次原生只返回 5 块；baseline 原生返回 10 块，本次
评估统一截前 5，因此主指标公平。但如果系统内部还允许重排或二次选择，PRIM
没有 Top-6～10 候选可用，会进一步限制文档内重排空间。

## 不是映射规则造成的假象

自动映射反而对 PRIM 略有利：含宽映射目标时 PRIM 块召回高于 baseline；去掉
所有宽映射题后，PRIM 的差距扩大。按目标映射难度拆分：

- 只有 1 个自动别名的 200 个目标：PRIM 48.00%，baseline 56.50%；
- 有 2–3 个别名的 415 个目标：59.52% vs 60.48%；
- 有 4 个以上别名的 275 个目标：75.27% vs 73.82%。

PRIM 最弱的是必须精确命中单一自动块的目标；候选别名越宽，PRIM 多调用带来的
覆盖优势越容易显现。这进一步指向块级定位精度不足，而非桥接漏配。

## 建议的改进顺序

1. 将 coverage 状态从“PE 项是否在答案里出现”改成“每个待判断论点是否有
   至少一条直接支撑它的 evidence obligation”，并让检索器能看到未覆盖列表。
2. 停止把 `new_evidence_id` 当成有效进展。只有新增 obligation 覆盖、或显著提高
   某 obligation 的证据置信度，才算 progress。
3. 一旦目标文档已进入结果，后续检索切换到 document-constrained 模式：限定
   `file_id`，使用具体章节词、阈值、药名和结论短语定位；必要时 open 相邻块。
4. 为每次调用增加 novelty gate：若 Top-5 大多重复已覆盖目标、或连续两次没有
   新增 obligation，则重写查询方向，而不是继续同义改写。
5. 查询规划采用“广度后深度”：先为每个药物、联合关系、剂量/禁忌/监测/替代
   各建立一个覆盖槽，再对高风险槽深入；不要在其他槽尚为空时连续深挖一个关系。
6. 评估时继续保留自主停止，主看 target recall；同时把
   `document-conditioned chunk recall`、`productive call rate` 和
   `late-call marginal gain` 加入开发诊断，避免文档 recall 掩盖文档内定位失败。

最值得先做的小规模消融是：不改 Agent 的调用上限，只加入
“正确文档命中后限定 file_id + 未覆盖 obligation 查询 + 邻块展开”。在典型失败
样本上跑 20–30 题，就能判断瓶颈主要来自底层块排序还是上层查询规划。

## 可复现产物

- `analyze_document_chunk_gap.py`：转化漏斗及调用归因脚本。
- `chunk_eval_output/document_chunk_gap_analysis.json`：175 条逐目标诊断结果。
- `document_eval_analysis/`：同一批记录重新生成的文档级评估结果。
