# Yuxi 项目审核交接说明（第二版）

本交接包是 2026-08-13 项目变更后的重新打包版本，用于交给独立负责人重新总结和审核。压缩包保存打包时工作区中的源码、实验脚本、设计文档、论文材料、评估数据及历史结果，不包含 Git 对象、本地依赖、缓存、日志或真实环境配置。

## 快照信息

- 当前分支：`sync-latest`
- 基准提交：`db62ca159fa1`
- 基准提交说明：`地图构建修复第七次`
- 打包时存在 11 个未纳入 Git 的 `paper/` 文件，均已按工作区实况收录。

## 已排除内容

- `.git/` 及版本控制内部数据；
- `.venv/`、`node_modules/` 等可重新安装的依赖；
- `__pycache__/`、`.pytest_cache/`、`.ruff_cache/`、`.uv-cache/` 等缓存；
- `dist/`、`build/` 等可再生成产物；
- `saves/`、日志、临时文件及本地运行状态；
- 真实 `.env`、密钥、凭据及其他私有配置；配置模板和示例仍保留；
- IDE、Codex 等本地工具状态；
- `deliverables/`，避免交接包递归包含自身和上一版归档。

压缩包内的 `PACKAGE_FILELIST.txt` 列出全部归档文件，外部 SHA-256 文件用于校验传输完整性。
