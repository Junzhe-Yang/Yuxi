# Yuxi V7 试验方案

日期：2026-08-20

依据：`yuxi升级方案V7.md`、现有 ACM-PRIM 与 Agentic RAG 的 175 题检索结果，以及当前 PRIM/ACM-PRIM 代码。

## 一、这轮实验只回答两个问题

V7 的核心判断是：当前 ACM-PRIM 经常在一两次检索后就认为调查已经完成，因此漏掉其它治疗要素或证据方面。

这轮实验只验证：

1. 单纯要求多检索几次，能否补回召回率；
2. 在检索次数相同的情况下，把查询分配给多个 Investigation，是否优于让 Agent 自由使用这些次数。

Top-25 用来辅助判断“查询方向正确但目标块排在第 11—25 名”的情况。它不直接作为主方法，也不默认把 25 个块全部交给 LLM。

本轮不修改 Atlas，不加入源块注入、BM25、RRF、reranker 或结构化最终答案。

## 二、Top-25 分成两种完全不同的用途

### 1. 后台 Top-25：主实验默认采用

Milvus 每次实际返回 25 个块，但：

- Agent 只看到前 10 个；
- 前 10 个按现有方式进入 Evidence Store；
- 第 11—25 个只保存在诊断 trace 中；
- 第 11—25 个不能被 Agent 阅读、引用，也不能影响 Investigation 状态。

这样可以同时计算 Recall@10 和 Recall@25，却不会增加 LLM 上下文，也不会诱导模型减少后续查询。

后台 Top-25 回答的是：

> ACM 漏掉的块已经被当前查询排到第 11—25 名，还是当前查询根本找不到它？

如果目标块出现在第 11—25 名，主要问题偏向排序或截断；如果 Top-25 仍然没有，主要问题偏向查询方向、调查广度或向量匹配。

### 2. 可见 Top-25：只做条件性小实验

直接给 Agent 展示 25 个块会同时改变三件事：

- 单次信息量增加；
- 上下文显著变长；
- 模型可能更早产生“证据已经够了”的感觉。

所以可见 Top-25 不进入主实验。只有后台 Top-25 表明第 11—25 名确实含有足够多的漏召目标后，才在小样本上测试。

当前 ACM 比 baseline 少命中 28 个金标目标块。若全部后台 Top-25 仍找不到至少 28 个新增目标，那么即使把第 11—25 名全部展示出来，也没有能力单靠扩大深度追平当前 Micro Recall，因而不再运行可见 Top-25。

## 三、先做不调用 LLM 的 Top-25 回放

从现有 ACM 和 baseline 记录中读取每一次真实查询，按照原始设置重新执行：

- 保留原查询文本；
- 保留 `global` 或 `document` 检索范围；
- 文档内查询保留原 `file_id`；
- 使用同一知识库、同一 embedding 和纯 Milvus 向量检索；
- 返回 Top-25。

这一步不生成新问题，不运行 Agent，也不把金标准交给检索器。金标准只在结果返回后由评估脚本匹配。

需要输出：

1. 原 Top-10 已命中的目标数；
2. 第 11—25 名新增命中的目标数；
3. 有新增目标的病例数；
4. 每个漏召目标在所有历史查询中的最好排名；
5. ACM 与 baseline 的 Recall@10、Recall@25；
6. 全库查询和文档内查询分别增加了多少目标。

结果按以下方式解释：

| 结果 | 说明 |
|---|---|
| 大量漏块位于第 11—25 名 | 当前查询方向基本正确，后续可研究排序或按需查看更多结果 |
| Top-25 增益很小 | 直接展示更多块没有意义，应处理调查广度和查询方向 |
| ACM Top-25 增益明显高于 baseline | ACM 查询可能更精确，但 Top-10 截断损失较大 |
| baseline Top-25 也同样增长 | 这是通用检索深度效应，不能算 V7 的方法增益 |

## 四、V7 主实验组

### A0：当前 ACM-PRIM

保持现有运行逻辑。Agent 每次看到 Top-10，自主决定调查数量、检索次数和停止时间。

历史 A0 可以复用，但必须先检查 `budgets.executed_search_calls`：如果没有任何病例达到当时的
20 次上限，说明该上限在这些轨迹中没有实际生效，可以直接复用；如果至少一例达到 20，
则 A0 和需要纳入同一比较的 baseline 都应使用与新实验相同的高上限重新运行。不能让一组
在 20 次处被截断、另一组允许继续到 50 次以上。新实验默认使用 50，若统一改为 100 或更高，
所有需要重跑的对照组也使用同一个值。

本次实施已扫描工作区现有导出记录：`acm-prim_ragrecords_425.json` 每例最多 9 次搜索，
`yuxi_vector_ragrecords_175.json` 最多 10 次，`prim_rag_full_rag_records.json` 最多 14 次，
均没有病例达到 20。因此就当前这三份结果而言，旧上限没有实际截断轨迹，可以复用；
后续换用其它历史文件时仍按上一段规则重新检查。

### A1：只增加最低检索次数

保持当前 Prompt、Investigation 和 Atlas 行为，不建立强制议程。Agent 在完成 6 次有效搜索以前不能结束。

如果 Agent 提前准备回答，Harness 只提示“尚未达到本实验的最低搜索次数，请选择仍值得核查的方面继续检索”。具体查什么仍由 Agent 决定。

这一组用于测量：只增加搜索努力能够带来多少收益。

### A2-K2：两个必需 Investigation，总搜索至少 6 次

Agent 先建立两个必需 Investigation，所有有效 PlanAnchor 至少绑定到其中一个 Investigation。

执行顺序：

1. 两个 Investigation 各完成一次初始探查；
2. 两个 Investigation 各完成一次互补探查；
3. 剩余至少两次搜索由 Agent 自主分配；
4. 总搜索次数至少 6；完成最低合同后仍可按证据缺口继续检索，默认运行保护上限为 50，且可统一调高。

### A2-K3：三个必需 Investigation，总搜索至少 6 次

Agent 先建立三个必需 Investigation。

执行顺序：

1. 三个 Investigation 各完成一次初始探查；
2. 三个 Investigation 各完成一次互补探查；
3. 第 7 次以后允许继续自主补充；
4. 总搜索次数至少 6，默认运行保护上限为 50，且可统一调高。

K2 和 K3 的总最低搜索次数都设为 6，因此二者主要比较调查广度，而不是调用数量。

### A3：阶段化上下文

A3 等于开发阶段胜出的 A2，再加 V7 中的阶段化上下文。它只在 A2 出现明显的长上下文问题时运行，例如互补查询大量重复、无效工具调用上升或模型持续围绕第一条证据打转。

A3 不作为首轮必跑组。

## 五、V7 调度需要补充的三个边界

### 1. 必需 Investigation 数量固定为 K

K2/K3 的用途是比较议程广度，因此必需 Investigation 数量必须固定，不能让每个病例由模型自行改变实验条件。

议程阶段固定创建 K 个“必需 Investigation”。完成最低两轮探查后，Agent 可以继续在现有 Investigation 中补查，直到认为没有值得继续核查的缺口或达到运行保护上限。默认上限设为 50，并允许所有对照组统一调高；它不表示建议 Agent 用满预算。

### 2. 技术失败不能吞掉努力次数

只有 `success` 和 `success_empty` 计入已完成搜索。`technical_failed` 允许按当前技术重试规则重试，不计入六次有效搜索。

### 3. 任何状态错误都不能阻止最终答案

达到最大预算、模型连续工具调用错误或部分 Investigation 未正常关闭时，系统仍必须生成 Markdown 最终答案，并在 trace 中标记 `contract_incomplete`。

不能因为议程或阶段状态不完整而杀掉会话，也不能只返回运行摘要。

## 六、开发与正式运行顺序

### 阶段 1：后台 Top-25 回放

对现有 175 题 ACM 和 baseline 查询执行 Top-25 回放。该阶段不调用 LLM。

评估前先修正文档评估器对 `success_empty` 的漏计。该修正只重算已有结果，不改变 Agent，也不要求重跑 A0 或 baseline。

### 阶段 2：小样本选择 K

从 175 题中固定选取 40 题：

- 20 题：ACM 搜索不超过 3 次且召回低于 baseline；
- 10 题：ACM 搜索不超过 3 次但与 baseline 持平；
- 10 题：ACM 搜索不少于 5 次且与 baseline 持平或领先。

运行 A1、A2-K2、A2-K3。A0 仅在历史轨迹没有触及 20 次上限时使用已有结果；否则按同一
高上限重跑。

这 40 题只用于发现明显失败和选择 K，不能作为正式效果结论。

K 的选择依次考虑：

1. 块召回和完整覆盖；
2. 互补探查新增目标数；
3. 重复查询与重复块；
4. 工具错误和未完成合同；
5. token、耗时和最终答案完整性。

### 阶段 3：剩余 135 题主实验

阶段 2 的 40 题不再用于主要效果比较。冻结 K 后，在其余 135 题上运行：

- A0：未触及旧上限时复用当前 ACM，否则使用统一高上限重跑；
- A1：最低六次搜索；
- A2：胜出的 K；
- 外部 Agentic RAG baseline：未触及旧上限时使用已有 Top-10 结果，否则使用统一高上限重跑。

A3 只有在阶段 2 明确发现上下文问题时才加入。

所有新运行在后台取 Top-25，但 Agent 默认只看 Top-10。可以另外汇总全部 175 题作为描述性结果，但论文中的开发后比较应优先报告未参与 K 选择的 135 题。

这 175 题此前已经参与多轮问题分析，因此 135 题仍不能视为完全未见测试集。它适合判断 V7 是否值得继续，不能单独承担最终确认结论。

### 阶段 4：可见 Top-25 小实验

只有阶段 1 表明第 11—25 名至少包含 28 个 ACM 新增目标时才执行。

在阶段 2 的同一批 40 题上增加一组：

- T25：采用 A1 的最低六次搜索规则，但每次向 Agent展示 25 个块。

T25 与 A1 的唯一差异是 Agent 可见深度 25 对 10。两组都有最低六次搜索，因此不会因为 T25 提前停止而直接少查；同时记录模型在达到六次以前尝试生成答案的次数，用来检验“结果越多，模型越想提前结束”的现象。

T25 只有同时满足以下条件才考虑进入后续方法：

1. Recall 和 Complete Coverage 明确高于 A1；
2. 最终回答质量不下降；
3. 重复查询没有明显增加；
4. 输入 token 和运行时间可以接受；
5. 提前作答倾向没有明显恶化，或即使恶化仍取得足够召回收益。

否则继续保持 Agent 可见 Top-10。

### 阶段 5：外部确认

若 A2 在 135 题上确实优于 A1，再使用未参与参数选择的新病例确认。现有 250 条 Beers 数据与前 175 题的病例构成不同，应作为单独的外部子集报告，不能与 175 题直接合并。该子集需要运行同配置的 Agentic RAG baseline、A1 和胜出的 A2。

## 七、所有组保持一致的条件

- 同一主模型和参数；
- 同一系统补充提示；
- 同一 Atlas 快照和完整静态文档地图；
- 同一 Milvus 知识库和 embedding；
- 纯向量检索，不使用 BM25 或 reranker；
- 最大搜索预算默认 50，允许所有对照组统一调高；
- 相同 open 预算；
- 相同 Evidence Card 长度；
- 相同 Markdown 最终答案要求；
- 每题独立 thread；
- 相同病例顺序和超时设置。

## 八、主要评价指标

### 检索结果

- Macro/Micro Recall@10；
- Complete Coverage@10；
- Target MRR@10、Target nDCG@10；
- 第一次搜索后的 Recall；
- Autonomous Recall AUC；
- Document-conditioned Chunk Recall；
- search-only 与 search+open 两种可访问证据召回。

### 后台 Top-25 诊断

- Recall@25；
- 第 11—25 名新增目标数；
- Top-10 漏召目标被 Top-25 找回的比例；
- 新增目标的排名分布；
- 全库和文档内查询各自的 Top-25 增益。

Recall@25 只用于判断原因，不能作为默认系统的主成绩，因为 Agent 没有看到第 11—25 名。

### V7 机制指标

- 每题 Investigation 数量；
- PlanAnchor 进入议程的比例；
- 未分组搜索率；
- 单查询关闭率；
- 初始探查和互补探查完成率；
- 互补探查新增金标目标数及零收益率；
- 查询文本相似度和重复块比例；
- 第 6 次以前的提前作答尝试次数；
- 搜索预算耗尽率；
- 合同未完成率；
- 输入 token、耗时和技术失败数。

### 最终回答

继续使用当前答案准确性和事实准确性评价。Markdown 格式不参与运行成败判断。另记录空答案、只有运行摘要、过程文字泄漏和 Evidence 引用错误。

## 九、结果怎么决定下一步

| 对比结果 | 结论 |
|---|---|
| A1 明显高于 A0 | 当前主要存在过早停止或搜索努力不足 |
| A1 与 A0 接近 | 单纯多查无效，不能继续用调用数量解释失败 |
| A2 明显高于 A1 | Investigation 议程和轮转调度具有独立价值 |
| A2 与 A1 接近 | V7 的复杂结构没有必要，收益主要来自多查 |
| A2 低于 A1 | 强制议程干扰 Agent，应停止 V7 合同方向 |
| K3 高召回但重复和成本明显恶化 | 优先 K2 |
| 后台 Top-25 增益大 | 当前查询存在排序/截断损失，可再研究按需展开或重排 |
| 后台 Top-25 增益小 | 目标块根本没有被当前查询找到，应改查询覆盖而非展示更多块 |
| T25 比 A1 更早尝试结束且召回无明显提高 | 拒绝可见 Top-25 |
| T25 在固定六次搜索下仍明显提高召回 | 才考虑更大可见深度或按需展开第 11—25 名 |

## 十、本轮明确不做

- 不重建 Atlas；
- 不自动选择或注入 Atlas cue；
- 不把 Atlas `source_chunk_ids` 塞入候选结果；
- 不做 BM25、RRF 或 reranker；
- 不强制结构化最终答案；
- 不在同一轮实验中修改现有 Markdown 覆盖反思逻辑；若后续修复，必须对所有 ACM 条件一致应用；
- 不依据金标准生成 Investigation 或查询；
- 不把 K、Top-K 或提示参数在正式测试结果出来后继续调整；
- 不把后台 Top-25 当成 Agent 实际召回成绩。

## 十一、推荐执行结论

先完成 Top-25 查询回放，它成本最低，也能直接判断是否存在明显的第 11—25 名排序损失。

V7 的主实验只需要 A1 与 A2：A1 控制搜索次数，A2检验 Investigation 组织是否额外有效。K2/K3 只在 40 题开发样本上选择一次。A3 和可见 Top-25 都是条件性实验，不应一开始全量运行。

这个顺序可以避免再次同时改变调查结构、检索深度、排序方法和最终答案格式，也能清楚判断每项改动是否真正有用。
